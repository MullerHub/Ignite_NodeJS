describe('criar categoria', () => {
  it('Espero que 2 + 2 seja 4', () => {
    const soma = 2 + 2
    const resultadoo = 4
    expect(soma).toBe(resultadoo)
  })
  it('Espero que 2 + 2 não seja 5', () => {
    const soma = 2 + 2
    const resultadoo = 5
    expect(soma).not.toBe(resultadoo)
  })
})
